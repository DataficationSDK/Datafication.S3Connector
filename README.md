# Datafication.S3Connector
S3 reading for DataBlocks—streaming I/O, automatic format detection (CSV, JSON, Parquet, Excel), and multi-segment mode for large datasets. Connects AWS S3 and S3-compatible services to the Datafication platform for cloud analytics and data lake workflows. This repo hosts install/usage docs and sample projects.
